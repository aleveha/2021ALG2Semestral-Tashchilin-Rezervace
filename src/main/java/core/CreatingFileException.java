package core;

public class CreatingFileException extends RuntimeException {
    /**
     * Creating file exception
     * @param msg error message
     */
    public CreatingFileException(String msg) {
        super(msg);
    }
}
